---
issue: 6
updated: 2026-08-04T00:00:00Z
status: planned
progress: 0%
---

# Execution Record: Issue #6

Implementation has not started. `../../6-analysis.md` is the controlling
plan: Stream A owns canonical evidence schemas and the pure exact-SHA
freshness/finding evaluator; Stream B owns the host-neutral review port and
the real separate ephemeral Codex-process adapter; Stream C owns ledger-gate
integration, status contribution, and focused review documentation/skill.

Start A and B in parallel. Start C's integration only after their public
contract inventories are published. No stream may modify existing context,
ledger, shared status projection, provider, workspace, promotion, or
finalization ownership without a recorded handoff. `src/index.ts` remains a
serialized post-stream export handoff.

Acceptance evidence and review records are authoritative only when the pure
gate validates complete records at the exact current product SHA. CCPM checked
boxes/execution state, GitHub approval, and raw task completion are
non-authoritative presentation facts and must never clear a blocker. An
accepted finding requires a resolution naming the current SHA and a renewed
independent review at that SHA.

Before completion, execute the non-circular bootstrap in the analysis: use
external fixture tests, independently observed exact-SHA command evidence, a
reviewer-only envelope, and a newly created Codex process/session. If the
candidate product SHA changes, regenerate acceptance evidence and rerun the
independent review. Record the final SHA, verifier, deterministic command
results, process-isolation evidence, finding dispositions, and renewed-review
result in durable evidence/review records; do not mark this execution record
or a GitHub issue state as an acceptance substitute.
