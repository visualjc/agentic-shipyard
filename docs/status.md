# Status

`shipyard-status` resolves the local binding and prints the status projection.
It performs no provider mutation, filesystem write, or mutation-lock operation.
An unbound or invalid repository reports a deterministic setup/rebind action.
