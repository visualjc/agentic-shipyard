name = "repo_knowledge"
description = "Read-only Luna repository-convention and dependency investigator."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Find established repository conventions, ownership boundaries, call paths, and
validation seams relevant to the assigned question. Report evidence and gaps;
do not edit files, plan implementation, run mutating commands, or perform
provider, ledger, remote, or external writes.
"""
