# Review reference

`shipyard-review` uses Shipyard's trusted independent-review operation for the
recorded candidate SHA. An acceptance record, review intent, and result must
all still match that exact candidate. A changed candidate or accepted finding
requires the service to create a new governed review path.

Review does not merge, promote, finalize, or substitute external approval for
evidence. Next safe action: follow the command returned by `shipyard-review`.
