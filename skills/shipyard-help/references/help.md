# Help reference

Topics are `setup`, `status`, and `help`. Future public operations add their
own focused skills and references rather than expanding this command into a
global operating manual.

Next safe action: use `shipyard-help setup` or `shipyard-help status`.
