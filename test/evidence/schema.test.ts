import assert from "node:assert/strict";
import test from "node:test";
import { canonicalJson, validateAcceptanceEvidence, validateEvidenceManifest, validateReviewRequest } from "../../src/evidence/schema.js";
import { EvidenceError } from "../../src/evidence/errors.js";
import { MAX_CANONICAL_JSON_BYTES } from "../../src/evidence/limits.js";
test("canonical JSON sorts object keys and rejects unknown evidence fields",()=>{assert.equal(canonicalJson({z:1,a:{y:2,b:3}}),'{"a":{"b":3,"y":2},"z":1}');assert.throws(()=>validateAcceptanceEvidence({schemaVersion:1,issueId:"6",productSha:"a".repeat(40),items:[],unexpected:true}),EvidenceError);});
test("hostile getters, proxies, sparse arrays, cycles, and non-JSON values fail closed",()=>{let touched=0;const getter=Object.defineProperty({},"x",{enumerable:true,get(){touched++;return 1;}});const proxy=new Proxy({},{ownKeys(){throw new Error("secret");}});const sparse=new Array(2);const cycle:any={};cycle.self=cycle;for(const value of [getter,proxy,sparse,cycle,undefined,NaN,Infinity])assert.throws(()=>canonicalJson(value),EvidenceError);assert.equal(touched,0);});
test("only exact SHA-1 or SHA-256 object IDs are accepted",()=>{for(const length of [41,63])assert.throws(()=>validateAcceptanceEvidence({schemaVersion:1,issueId:"6",productSha:"a".repeat(length),items:[]}),EvidenceError);});
test("review requests require nonempty intent and evidence references plus exact bundle pins",()=>{const request={schemaVersion:1,issueId:"6",productSha:"a".repeat(40),reviewId:"r",reviewerEnvelopePath:"reviewer.json",intentRefs:["intent.md"],evidenceRefs:["acceptance.json"],reviewedLedgerSha:"b".repeat(40),manifestDigest:"c".repeat(64),acceptanceDigest:"d".repeat(64)};assert.throws(()=>validateReviewRequest({...request,intentRefs:[]}),EvidenceError);assert.throws(()=>validateReviewRequest({...request,evidenceRefs:[]}),EvidenceError);assert.throws(()=>validateReviewRequest({...request,acceptanceDigest:"bad"}),EvidenceError);});
test("versioned manifests support arbitrary future issue IDs without a compiled registry",()=>{const manifest=validateEvidenceManifest({schemaVersion:1,issueId:"JG-8421",items:[{id:"future-contract",kind:"acceptance"}]});assert.equal(manifest.issueId,"JG-8421");assert.throws(()=>validateEvidenceManifest({...manifest,schemaVersion:2}),EvidenceError);});
test("canonicalization rejects hostile oversized valid JSON before unbounded serialization",()=>{assert.throws(()=>canonicalJson({value:"x".repeat(MAX_CANONICAL_JSON_BYTES+1)}),EvidenceError);});
