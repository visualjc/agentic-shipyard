import assert from "node:assert/strict";
import test from "node:test";
import { CodexReviewAdapter } from "../../src/adapters/codex-review.js";
import { ReviewError } from "../../src/review/errors.js";
import { mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
const sha="a".repeat(40), now="2026-08-04T00:00:00.000Z", request={schemaVersion:1 as const,issueId:"6",productSha:sha,reviewId:"r-1",reviewerEnvelopePath:".shipyard/reviewer.json",intentRefs:["intent.md"],evidenceRefs:["acceptance.json"]};
const config={executable:"/fake-codex",runtimePath:"/usr/bin:/bin",codeHome:"/isolated/codex",model:"test-model",profile:"test-profile"};
test("Codex adapter supplies reviewer-only bounded invocation and verifies a fresh PID",async()=>{let input:any;const runner={run:async(v:any)=>{if(v.args[0]==="--version")return {processId:98,sessionId:"probe",exitCode:0,stdout:"codex-test-1",stderr:""};input=v;await writeFile(v.args[v.args.indexOf("-o")+1],JSON.stringify({reviewId:"r-1",productSha:sha,reviewer:"codex",startedAt:now,finishedAt:now,findings:[],successful:true}));return {processId:99,sessionId:"child",exitCode:0,stdout:"",stderr:""};}};const out=await new CodexReviewAdapter(config,runner).review({host:"codex",role:"reviewer",reviewRequestPath:"deliveries/d/evidence/review-request-r-1.json",reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request);assert.equal(out.attestation.processId,99);assert.equal(out.attestation.commandVersion,"codex-test-1");assert.deepEqual(Object.keys(input.env).sort(),["CODEX_HOME","PATH","SHIPYARD_REVIEW_SESSION","SHIPYARD_REVIEW_SESSION_DIR"]);assert.ok(input.args.includes("--ephemeral")&&input.args.includes("--output-schema"));assert.ok(!input.stdin.includes("IMPLEMENTER_SESSION"));});
test("wrong role rejects before spawn",async()=>{let runs=0;const runner={run:async()=>{runs++;throw new Error("must not run");}};await assert.rejects(new CodexReviewAdapter(config,runner).review({host:"codex",role:"implementer" as never,reviewRequestPath:"deliveries/d/evidence/review-request-r-1.json",reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request),ReviewError);assert.equal(runs,0);});
test("a synthetic executable is a separate fresh reviewer process with no inherited implementer identity",async()=>{const root=await mkdtemp(join(tmpdir(),"shipyard-child-"));try{const requestPath="deliveries/d/evidence/review-request-r-1.json", envelopePath=join(root,"reviewer.json"), localRequest={...request,reviewerEnvelopePath:envelopePath};await writeFile(join(root,"request.json"),JSON.stringify(localRequest));await writeFile(envelopePath,"reviewer only");process.env.IMPLEMENTER_SESSION="implementer-secret";process.env.CODEX_SESSION="parent-secret";const executable=join(process.cwd(),"test","review","helpers","fake-process.mjs");const adapter=new CodexReviewAdapter({...config,executable,runtimePath:process.env.PATH!,codeHome:root});const first=await adapter.review({host:"codex",role:"reviewer",reviewRequestPath:requestPath,reviewerEnvelopePath:envelopePath,repoRoot:root},localRequest);const observed=JSON.parse(await readFile(join(root,".fixture-observed.json"),"utf8"));assert.notEqual(observed.pid,process.pid);assert.equal(observed.pid,first.attestation.processId);assert.equal(observed.implementer,undefined);assert.equal(observed.parent,undefined);assert.equal(observed.envelopePath,envelopePath);assert.ok(!observed.args.includes(envelopePath) && !observed.args.includes(requestPath));assert.ok(typeof observed.sessionDir==="string");assert.equal(await (await import("node:fs/promises")).stat(observed.sessionDir).then(()=>true,()=>false),false);const second=await adapter.review({host:"codex",role:"reviewer",reviewRequestPath:requestPath,reviewerEnvelopePath:envelopePath,repoRoot:root},localRequest);assert.notEqual(first.attestation.processId,second.attestation.processId);assert.notEqual(first.attestation.sessionId,second.attestation.sessionId);}finally{delete process.env.IMPLEMENTER_SESSION;delete process.env.CODEX_SESSION;await rm(root,{recursive:true,force:true});}});
test("nonzero, timeout, oversize, and spawn failures are redacted and fail closed",async()=>{for(const run of [{processId:1,sessionId:"x",stdout:"",stderr:"secret",exitCode:7},{processId:1,sessionId:"x",stdout:"",stderr:"",exitCode:0,timedOut:true},{processId:1,sessionId:"x",stdout:"",stderr:"",exitCode:0,oversize:true}])await assert.rejects(new CodexReviewAdapter(config,{run:async()=>run}).review({host:"codex",role:"reviewer",reviewRequestPath:"deliveries/d/evidence/review-request-r-1.json",reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request),(e:any)=>e instanceof ReviewError&&!e.message.includes("secret"));await assert.rejects(new CodexReviewAdapter(config,{run:async()=>{throw new Error("credential-secret")}}).review({host:"codex",role:"reviewer",reviewRequestPath:"deliveries/d/evidence/review-request-r-1.json",reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request),(e:any)=>e instanceof ReviewError&&!e.message.includes("credential"));});
test("oversized result files and unsafe paths fail closed",async()=>{let runs=0;const runner={run:async(v:any)=>{runs++;if(v.args[0]==="--version")return {processId:1,sessionId:"probe",exitCode:0,stdout:"codex-test-1",stderr:""};await writeFile(v.args[v.args.indexOf("-o")+1],Buffer.alloc(1_000_001));return {processId:2,sessionId:"child",exitCode:0,stdout:"",stderr:""};}};await assert.rejects(new CodexReviewAdapter(config,runner).review({host:"codex",role:"reviewer",reviewRequestPath:"deliveries/d/evidence/review-request-r-1.json",reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request),/oversized/);for(const path of ["/absolute.json","../foreign.json","implementer/assigned-task.md"])await assert.rejects(new CodexReviewAdapter(config,runner).review({host:"codex",role:"reviewer",reviewRequestPath:path,reviewerEnvelopePath:".shipyard/reviewer.json",repoRoot:"/repo"},request),ReviewError);assert.equal(runs,2);});
