import assert from "node:assert/strict";
import test from "node:test";
import { TrustedReviewDispatcher } from "../../src/review/dispatch.js";
import { ReviewError } from "../../src/review/errors.js";
const sha="a".repeat(40), request={schemaVersion:1 as const,issueId:"6",productSha:sha,reviewId:"r",reviewerEnvelopePath:"reviewer.json",intentRefs:["intent.md"],evidenceRefs:["acceptance.json"]};
const envelope:any={deliveryId:"d",role:"reviewer",host:"codex",productSha:sha,adapter:{envelopePath:"reviewer.json",repoRoot:"/repo"}};
test("trusted dispatch loads and seals the pinned reviewer bundle before adapter invocation",async()=>{let loaded=0,called=0,bundle:any;const records={"deliveries/d/intent.md":"intent","deliveries/d/acceptance.json":"acceptance","deliveries/d/review.json":"review"};const bridge=new TrustedReviewDispatcher({load:async()=>{loaded++;return {envelope,records};}} as any,{codex:{review:async(dispatch:any)=>{called++;bundle=JSON.parse(dispatch.sealedBundle);return {result:{},attestation:{}} as any;}}});await bridge.dispatch(envelope,"deliveries/d/evidence/review-request-r.json",request);assert.equal(loaded,1);assert.equal(called,1);assert.deepEqual(bundle.records,records);assert.equal(bundle.request.reviewId,"r");assert.equal(bundle.envelope.productSha,sha);});
test("stale/wrong-role context rejects before adapter invocation",async()=>{let called=0;for(const changed of [{...envelope,role:"implementer"},{...envelope,productSha:"b".repeat(40)}]){const bridge=new TrustedReviewDispatcher({load:async()=>({envelope:changed,records:{}})} as any,{codex:{review:async()=>{called++;return {} as any;}}});await assert.rejects(bridge.dispatch(changed,"deliveries/d/evidence/review-request-r.json",request),ReviewError);}assert.equal(called,0);});
test("foreign, absolute, traversal, and implementer request paths never spawn",async()=>{let called=0;const bridge=new TrustedReviewDispatcher({load:async()=>({envelope,records:{}})} as any,{codex:{review:async()=>{called++;return {} as any;}}});for(const path of ["/tmp/request.json","../request.json","deliveries/other/evidence/review-request-r.json","deliveries/d/assigned-task.md"])await assert.rejects(bridge.dispatch(envelope,path,request),ReviewError);assert.equal(called,0);});
test("non-Codex trusted envelopes are unsupported with zero spawn",async()=>{let called=0;const other={...envelope,host:"other"};const bridge=new TrustedReviewDispatcher({load:async()=>({envelope:other,records:{}})} as any,{other:{review:async()=>{called++;return {} as any;}}});await assert.rejects(bridge.dispatch(other,"deliveries/d/evidence/review-request-r.json",request),(e:any)=>e instanceof ReviewError&&e.code==="review-unsupported-host");assert.equal(called,0);});
