---
name: slipway-promote
description: Preflight and, when explicitly authorized, cherry-pick exact reviewed product-only commits from an agentic work branch to the clean delivery PR branch. Use for `$slipway-promote`, initial delivery PR creation, or updating the same delivery PR after agentic feedback fixes.
---

# Slipway promote

Preflight the canonical `slipway` skill in the current host. If it is absent, block and require installation of the complete Slipway suite; do not transfer cargo from this entry point alone.

Invoke `$slipway` with the forced `promotion` operation, exact work branch, reviewed SHA, and ordered cargo commits. Require the primary coordinator to default to mutation-free preflight, reject any materialized worktree-root private-overlay path or private policy leaked into public instructions without rejecting reviewed Slipway template assets, verify and authorize the exact target and recovery path, transfer only product cargo, and return the delivery PR to its protected human merge workflow.
