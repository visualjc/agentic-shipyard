# CodeGraph context

Activate this module only when the repository root contains `.codegraph/` and
the CodeGraph MCP tool or `codegraph` command is available.

When locating or understanding code, use CodeGraph before grep, find, or broad
file reading. Name the relevant symbol or file in the query and use the
returned source and call paths to guide narrower inspection.
