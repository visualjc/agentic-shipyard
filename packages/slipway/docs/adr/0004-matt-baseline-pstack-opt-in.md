# Use Matt skills as the baseline builder

Slipway requires Matt Pocock's planning and implementation skills as its complete baseline and treats pstack as an explicit opt-in build provider. This gives every supported host a coherent idea-to-review path without embedding CCPM's overlapping orchestration, while allowing projects or individual runs to add pstack fan-out when its extra coordination is worth the cost.
